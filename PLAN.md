# AI 즉흥 여행 추천 시스템 — 설계 계획서

---

## 1. 프로젝트 목표

"지금 당장 누구랑, 어디서, 뭐 하고 싶다"는 즉흥적 상황에서
충남 천안/아산 지역의 최적 장소를 추천하는 앱.

추천 모드 두 가지:
- **지금 당장 뭘 할지** — 한 종류의 장소 즉시 탐색 (밥집만, 카페만 등), 상위 N곳 반환
- **오늘 뭘 할지** — 음식→활동→카페→활동→음식 5-stop 당일 코스 추천 (최대 5코스)

---

## 2. 사용자 입력 (Flutter)

| 입력 | 방식 |
|------|------|
| 지역 선택 | 내 주변 (GPS) / 천안 / 아산 |
| 교통수단 | 차 / 대중교통 |
| 대중교통 허브 | 지역 선택 시 출발 역/터미널 직접 선택 |
| 페르소나 태그 | 6개 그룹 × 총 53개 태그 칩 선택 |
| 자유 텍스트 | 텍스트 입력 + "태그 자동 선택" 버튼 |
| 추천 모드 | 지금 당장 / 오늘 하루 |
| 결과 개수 | spot: 3/5/7/10 / course: 1/2/3/4/5 |

### 탐색 중심점
- **내 주변**: 현재 GPS 위치 기반 탐색
- **천안/아산 + 차**: 지역 중심 좌표 기준 반경 15km
- **천안/아산 + 대중교통**: 사용자가 직접 선택한 역/터미널 좌표 기준 반경 5km

### 대중교통 허브 선택지
- 천안: 천안역, 봉명역, 쌍용역, 두정역, 천안아산역(KTX), 천안종합터미널
- 아산: 온양온천역, 신창역, 탕정역, 천안아산역(KTX), 아산터미널, 배방역

### 페르소나 태그 그룹 (총 53개)
| 그룹 | 태그 예시 |
|------|---------|
| 누구와 | 혼자, 친구와, 연인과, 가족 나들이, 아이와 함께 ... |
| 어떤 분위기 | 힐링/여유, 활동적인, 감성적인, 로맨틱한, 트렌디한 ... |
| 특별한 날 | 기념일, 생일, 프로포즈, 첫 데이트 ... |
| 실내·야외 | 실내 위주, 야외 위주 |
| 어떤 활동 | 맛집 탐방, 카페 투어, 문화·역사 탐방, 드라이브 ... |
| 기타 | 반려동물 동반, 가성비, 어르신 동반 ... |

### 자유 텍스트 → 태그 자동 선택
- 텍스트 입력 후 "태그 자동 선택" 버튼 클릭
- Claude가 텍스트를 분석해 어울리는 페르소나 태그를 반환 → UI에서 자동 선택
- 자동 선택된 태그는 수동 수정 가능

---

## 3. Claude API 런타임 활용 — 3단계 분리 호출

### 호출 1: 텍스트 → 페르소나 태그 제안 (`POST /suggest/tags`)
사용자 자유 텍스트 → Claude Haiku → 53개 태그 중 어울리는 것 반환

```json
{"tags": ["연인과", "로맨틱한", "첫 데이트"]}
```

### 호출 2: 텍스트 → 카테고리 필터 추출 (spot 모드 전용)
사용자 자유 텍스트 → Claude Haiku → 장소 종류 키워드 반환

```json
{"category_filters": ["닭갈비"]}
```

규칙: 사용자가 명시한 장소 종류만 포함. 상위/유사 카테고리 추가 금지.

### 호출 3: 페르소나 태그 → 블로그 검색 키워드 확장
선택된 페르소나 태그 → Claude Haiku → 네이버 블로그 실제 검색어로 확장

```json
{"blog_tag_keywords": ["연인", "데이트", "커플", "로맨틱"]}
```

---

## 4. 추천 알고리즘 (Flask)

### 공통 전처리
```
① 반경 필터링
   탐색 중심점 기준 교통수단별 반경 내 장소 추출
   - 차:        반경 15km
   - 대중교통:  반경 5km

② 키워드 필터 적용
   category_filters: 카테고리 컬럼 부분 매칭
   blog_tag_keywords: blog_tags.csv 태그 부분 문자열 매칭
   AND 교집합 우선, 결과 3개 미만 시 category_filters만 적용 fallback

③ 랭킹 점수 계산
   인기도_raw  = log(방문자수 + 블로그수 × 0.3 + 1)
   인기도_norm = (인기도_raw - min) / (max - min)   # 0~1
   태그_매칭_점수 = (블로그 태그 키워드 매칭 수) / len(blog_tag_keywords)  # 0~1
   최종점수 = 0.4 × 인기도_norm + 0.6 × 태그_매칭_점수
```

### 모드 1 — "지금 당장 뭘 할지" (`/recommend/spot`)
```
④ 최종점수 상위 N곳 선택 (N = 사용자 설정, 기본 5, 최대 20)
⑤ Greedy TSP로 방문 순서 최적화 (출발점 기준 nearest-neighbor)
```

### 모드 2 — "오늘 뭘 할지" (`/recommend/course`)
```
④ K-Means 지리 군집화  [ML]
   반경 내 장소를 지리 좌표 기반으로 클러스터링 (Silhouette Score 최적 k).
   클러스터 하나 = 코스 하나 → 5개 stop이 같은 동네에 모임.
   클러스터에 해당 카테고리 없으면 전체 반경으로 fallback.

⑤ 고정 5-stop 템플릿으로 코스 구성
   템플릿: 음식점 → 활동A → 카페 → 활동B(다른 그룹) → 음식점(다른 카테고리)

⑥ 활동 그룹 5개로 코스 다양성 확보
   그룹 0: 공원/산책로/하천
   그룹 1: 자연공원/관광지/온천/캠핑
   그룹 2: 박물관/전시/미술관/문화재/유적
   그룹 3: 시장/쇼핑/체험
   그룹 4: 키즈카페/노래방/볼링

   코스별 (활동A 그룹, 활동B 그룹): (0,2), (1,3), (4,0), (3,4), (2,1)

⑦ 코스 다양성 강제
   - 음식점 최대 3곳 (점심+저녁+카페 구조)
   - 같은 음식 카테고리 반복 방지
   - 코스 간 장소 중복 방지 (globally_used set)
   - 후보 없을 시 단계적 fallback (태그 완화 → 카테고리 완화)
```

---

## 5. 데이터 파이프라인 현황

| 단계 | 상태 | 결과 |
|------|------|------|
| 장소 크롤링 (Selenium) | ✅ 완료 | 13,784개 |
| 카테고리 필터링 | ✅ 완료 | 486개 제거 |
| 블로그 태그 수집 | ✅ 완료 | 12,034개 → `blog_tags.csv` |
| Flask 알고리즘 구현 | ✅ 완료 | `app.py`, `recommendation_ai.py` |
| Flutter UI 구현 | ✅ 완료 | `input_screen.dart`, `result_screen.dart` |

---

## 6. 기술 스택

| 영역 | 기술 |
|------|------|
| 텍스트 → 카테고리/태그 추출 (런타임) | Claude Haiku API (`claude-haiku-4-5-20251001`) |
| 경로 최적화 | Greedy TSP (nearest-neighbor) |
| 장소 크롤링 | Selenium + BeautifulSoup |
| 블로그 태그 수집 | Naver Blog Search API |
| API 게이트웨이 | Node.js + Express (port 8080) |
| 추천 엔진 | Flask + pandas + geopy (port 5000) |
| 클라이언트 | Flutter (Android) |

---

## 7. 데이터 파일 역할

| 파일 | 역할 |
|------|------|
| `chungnam_places_filtered.csv` | 장소 목록 (카테고리, 좌표, 방문자수, 블로그수) |
| `blog_tags.csv` | 장소별 블로그 태그 빈도 JSON — 런타임 키워드 검색 인덱스 |
