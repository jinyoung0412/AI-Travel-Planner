# AI 즉흥 여행 추천 시스템 — 설계 계획서

---

## 1. 프로젝트 목표

"지금 당장 누구랑, 어디서, 뭐 하고 싶다"는 즉흥적 상황에서
충남 천안/아산 지역의 최적 장소를 추천하는 앱.

추천 모드 두 가지:
- **지금 당장 뭘 할지** — 한 종류의 장소 즉시 탐색 (밥집만, 카페만 등)
- **오늘 뭘 할지** — 다양한 카테고리로 구성된 3-stop 당일 코스 추천

---

## 2. 사용자 입력 (Flutter)

| 입력 | 방식 |
|------|------|
| 추천 모드 | 지금 당장 / 오늘 하루 |
| 탐색 중심점 | GPS 자동 or 지역 수동 선택 |
| 교통수단 | 차 / 대중교통 |
| 요청 | 자유 텍스트 입력 (자연어) |

### 탐색 중심점
- **GPS 자동**: 현재 위치 기반 탐색
- **수동 선택**: 가고 싶은 지역 직접 선택 (천안역, 쌍용역, 온양온천역 등)
- 출발지와 중심점이 달라도 무관 — 사용자가 이동을 감수하고 선택한 것이므로 패널티 없음

### 이동 시간 사전 체크 (카카오 모빌리티 API)
현재 위치 → 탐색 중심점 이동 시간 계산
- 30분 이내 → 바로 추천 진행
- 30~60분 → "약 N분 거리입니다. 계속할까요?" 경고
- 60분 초과 → "이동이 오래 걸립니다. 더 가까운 [대안 지역]은 어떠세요?" 제시

### 자유 텍스트 입력 예시
- "부모님과 함께 고기집 가고 싶어요"
- "연인이랑 분위기 좋은 카페 추천해줘"
- "친구들이랑 술 한잔 할 수 있는 데"
- "혼자 조용히 산책하고 싶어"

---

## 3. Claude API 런타임 활용 — 필수 키워드 추출

사용자 자연어 입력 → Claude Haiku가 두 종류의 키워드 추출:

### 추출 결과 구조
```json
{
  "category_filters": ["육류", "고기"],
  "blog_tag_keywords": ["부모님", "가족", "어버이"]
}
```

| 키워드 종류 | 역할 | 예시 |
|-------------|------|------|
| `category_filters` | `chungnam_places_filtered.csv` 카테고리 컬럼 필터 | "고기집" → ["육류", "고기"] |
| `blog_tag_keywords` | `blog_tags.csv` 태그 부분 문자열 매칭 | "부모님" → ["부모님", "가족", "어버이"] |

### 입력→키워드 매핑 예시

| 사용자 입력 | category_filters | blog_tag_keywords |
|------------|-----------------|-------------------|
| "부모님과 고기집" | ["육류", "고기"] | ["부모님", "가족", "어버이"] |
| "연인이랑 카페" | ["카페"] | ["데이트", "연인", "커플"] |
| "친구들이랑 술 한잔" | ["술집", "호프", "이자카야"] | ["회식", "단체", "모임"] |
| "혼자 산책" | ["공원", "산책"] | ["혼자", "혼밥", "1인"] |
| "아이랑 갈 만한 곳" | [] | ["아이", "아이랑", "어린이", "가족"] |

### 필터 적용 방식
```
① category_filters → 카테고리 컬럼 부분 매칭 (OR)
② blog_tag_keywords → 블로그 태그 JSON 내 부분 문자열 매칭 (OR)
③ 두 조건 모두 충족하는 장소 우선 (AND)
   → 결과 부족 시 fallback: category_filters만 적용 + 블로그 태그 매칭 점수로 정렬
```

> 네이버 블로그 태그는 복합 형태로 저장됨 ("부모님과외식", "가족나들이" 등).
> 부분 문자열 검색(substring match)으로 처리.

---

## 4. 추천 알고리즘 (Flask) — 모드별 분리

### 공통 전처리
```
① 반경 필터링
   탐색 중심점 기준 교통수단별 반경 내 장소 추출
   - 차:        반경 15km
   - 대중교통:  반경 5km

② 필수 키워드 필터 적용
   category_filters: 카테고리 컬럼 부분 매칭
   blog_tag_keywords: blog_tags.csv 태그 부분 문자열 매칭
   AND 교집합 우선, 부족 시 OR fallback

③ 랭킹 점수 계산
   인기도_raw  = log(방문자수 + 블로그수 × 0.3 + 1)
   인기도_norm = (인기도_raw - min) / (max - min)   # 0~1
   태그_매칭_점수 = (블로그 태그 키워드 매칭 수) / len(blog_tag_keywords)  # 0~1
   최종점수 = 0.4 × 인기도_norm + 0.6 × 태그_매칭_점수
```

### 모드 1 — "지금 당장 뭘 할지" (`/recommend/spot`)
```
④ 최종점수 기반 상위 5곳 반환
   - 단일 카테고리 집중 (음식점만, 카페만, 관광지만 등)
   - K-Means 불필요, 가까운 순 + 점수 순 정렬
```

### 모드 2 — "오늘 뭘 할지" (`/recommend/course`)
```
④ K-Means 지리 군집화  [ML]
   후보 장소를 지리적으로 k개 군집으로 묶음
   → 점수 가장 높은 군집 선택

⑤ 카테고리 다양성 강제
   3-stop 구성 규칙:
   - 음식점(맛집/카페 포함) 최대 2곳
   - 자연/문화/액티비티 최소 1곳
   - 같은 대분류 카테고리 중복 지양

⑥ Greedy TSP 경로 최적화
   교통수단별 장소 간 이동 패널티 적용
   - 대중교통: 10km 초과 시 3배 패널티
   - 차:       거리 감쇄 0.5배
```

---

## 5. 데이터 파이프라인 현황

| 단계 | 상태 | 결과 |
|------|------|------|
| 장소 크롤링 (Selenium) | ✅ 완료 | 13,784개 |
| 카테고리 필터링 | ✅ 완료 | 486개 제거 |
| 블로그 태그 수집 | ✅ 완료 | 12,034개 → `blog_tags.csv` |
| ~~페르소나 매핑 (Claude API)~~ | ❌ 제거됨 | 런타임 방식으로 대체 |
| Flask 알고리즘 재설계 | ⬜ 미착수 | `app.py` |
| Flutter UI 업데이트 | ⬜ 미착수 | 모드 선택, 지역선택, 텍스트 입력 화면 |

---

## 6. 구현 순서

**Step 1 — Flask 알고리즘 재설계** (`app.py`)
- Claude Haiku 런타임 호출: 자연어 → `{category_filters, blog_tag_keywords}` 추출
- 공통: 반경 필터 → 키워드 필터 → 점수 계산
- 모드 1 (`/recommend/spot`): 상위 5곳 반환
- 모드 2 (`/recommend/course`): K-Means 군집화 + 카테고리 다양성 + TSP 경로 최적화
- blog_tags.csv 로드 및 부분 문자열 매칭 유틸리티

**Step 2 — Flutter UI 업데이트**
- 홈에서 모드 선택 (지금 당장 / 오늘 하루)
- 탐색 중심점 선택 UI (GPS / 수동)
- 교통수단 선택
- 자유 텍스트 입력 창 (단일 입력)
- 이동 시간 경고 로직 (카카오 모빌리티 API)

---

## 7. 기술 스택

| 영역 | 기술 |
|------|------|
| 텍스트 → 필수 키워드 추출 (런타임) | Claude Haiku API (`claude-haiku-4-5-20251001`) |
| 지리 군집화 | K-Means (scikit-learn) |
| 경로 최적화 | Greedy TSP |
| 이동 시간 계산 | 카카오 모빌리티 API |
| 장소 크롤링 | Selenium + BeautifulSoup |
| 블로그 태그 수집 | Naver Blog Search API |

---

## 8. 데이터 파일 역할

| 파일 | 역할 |
|------|------|
| `chungnam_places_filtered.csv` | 장소 목록 (카테고리, 좌표, 방문자수, 블로그수) |
| `blog_tags.csv` | 장소별 블로그 태그 빈도 JSON — 런타임 키워드 검색 인덱스 |
